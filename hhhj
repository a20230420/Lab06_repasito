package com.example.demo.entity;

import lombok.AllArgsConstructor;
import lombok.Getter;
import lombok.NoArgsConstructor;
import lombok.Setter;

import java.io.Serializable;

// NO es @Entity — se guarda en sesión, no en BD
@Getter @Setter @NoArgsConstructor @AllArgsConstructor
public class Cancion implements Serializable {

    private Long id;
    private String nombre;
    private String artista;
    private String genero;
    private String duracion;
}

package com.example.demo.entity;

import lombok.AllArgsConstructor;
import lombok.Getter;
import lombok.NoArgsConstructor;
import lombok.Setter;

import java.io.Serializable;
import java.time.LocalDateTime;

// NO es @Entity — se guarda en sesión, no en BD
@Getter @Setter @NoArgsConstructor @AllArgsConstructor
public class PlaylistItem implements Serializable {

    private Cancion cancion;
    private LocalDateTime fechaAgregado;
    private String estado; // "Agregado" o "Eliminado"
}


package com.example.demo.repository;

import com.example.demo.entity.User;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import java.util.Optional;

@Repository
public interface UserRepository extends JpaRepository<User, Long> {
    Optional<User> findByUsername(String username);
}

llzz
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Canciones</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
</head>
<body>

<nav th:replace="~{layout/base :: navbar}"></nav>

<div class="container mt-4">
    <h2>Gestión de Canciones</h2>

    <!-- Formulario nueva canción -->
    <form th:object="${cancion}"
          th:action="@{/canciones/guardar}"
          method="post" class="mb-4">

        <div class="row g-2">
            <div class="col">
                <input type="text" th:field="*{nombre}"
                       class="form-control" placeholder="Nombre *">
            </div>
            <div class="col">
                <input type="text" th:field="*{artista}"
                       class="form-control" placeholder="Artista *">
            </div>
            <div class="col">
                <input type="text" th:field="*{genero}"
                       class="form-control" placeholder="Género *">
            </div>
            <div class="col">
                <input type="text" th:field="*{duracion}"
                       class="form-control" placeholder="Duración *">
            </div>
            <div class="col-auto">
                <button type="submit" class="btn btn-primary">
                    Agregar
                </button>
            </div>
        </div>
    </form>

    <!-- Lista -->
    <table class="table table-bordered">
        <thead class="table-dark">
            <tr>
                <th>#</th>
                <th​​​​​​​​​​​​​​​​

